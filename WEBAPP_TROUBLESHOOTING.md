# Диагностика проблем с Telegram Web App

## Возможные причины, почему Web App не работает:

### 1. **HTTPS обязателен для Telegram Web App**

Telegram Web App требует HTTPS URL (кроме localhost для разработки).

**Решение:**
- Убедитесь, что `WEBHOOK_URL` в `.env` использует HTTPS:
  ```
  WEBHOOK_URL=https://your-domain.com
  ```
- НЕ используйте HTTP в production:
  ```
  ❌ WEBHOOK_URL=http://your-domain.com  # Не работает!
  ✅ WEBHOOK_URL=https://your-domain.com  # Правильно
  ```

### 2. **URL должен быть доступен извне**

Web App должен быть доступен по публичному URL, а не только локально.

**Проверка:**
```bash
# На сервере проверьте доступность файла
curl https://your-domain.com/webapp/privacy_policy.html

# Должен вернуть HTML содержимое файла
```

### 3. **Проверка пути к файлам**

**На сервере выполните:**
```bash
# Проверьте, что файлы существуют
docker-compose exec app ls -la /app/src/presentation/web_app/

# Должны быть файлы:
# - privacy_policy.html
# - style.css
```

**Проверьте через тестовый endpoint:**
```bash
curl http://localhost:8000/webapp/test
# Или через Nginx:
curl https://your-domain.com/webapp/test
```

### 4. **Проверка Nginx конфигурации**

Убедитесь, что Nginx правильно проксирует запросы к `/webapp`:

```bash
# Проверьте конфигурацию Nginx
docker-compose exec nginx cat /etc/nginx/nginx.conf

# Перезапустите Nginx при необходимости
docker-compose restart nginx
```

### 5. **Проверка логов**

```bash
# Логи приложения
docker-compose logs app | grep -i webapp

# Логи Nginx
docker-compose logs nginx

# Проверьте, что путь монтируется правильно
docker-compose logs app | grep "Web App files mounted"
```

### 6. **Проверка переменных окружения**

```bash
# На сервере проверьте .env файл
docker-compose exec app env | grep WEBHOOK_URL

# Убедитесь, что WEBHOOK_URL установлен и использует HTTPS
```

## Пошаговая диагностика:

### Шаг 1: Проверка доступности файла напрямую

```bash
# Через порт приложения (внутри Docker сети)
curl http://localhost:8000/webapp/privacy_policy.html

# Через Nginx (публичный URL)
curl https://your-domain.com/webapp/privacy_policy.html
```

**Ожидаемый результат:** HTML код страницы политики конфиденциальности

### Шаг 2: Проверка тестового endpoint

```bash
curl https://your-domain.com/webapp/test
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "webapp_path": "/app/src/presentation/web_app",
  "exists": true,
  "files": ["privacy_policy.html", "style.css"]
}
```

### Шаг 3: Проверка в браузере

Откройте в браузере:
```
https://your-domain.com/webapp/privacy_policy.html
```

**Ожидаемый результат:** Страница должна отображаться корректно

### Шаг 4: Проверка в Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите на кнопку "📄 Ознакомиться с политикой"
4. Web App должен открыться

**Если не открывается:**
- Проверьте логи бота на ошибки
- Убедитесь, что URL использует HTTPS
- Проверьте, что домен доступен извне

## Частые ошибки:

### Ошибка: "Failed to open Web App"

**Причина:** URL недоступен или использует HTTP вместо HTTPS

**Решение:**
1. Установите `WEBHOOK_URL` с HTTPS
2. Убедитесь, что домен доступен извне
3. Проверьте SSL сертификат

### Ошибка: "404 Not Found"

**Причина:** Файл не найден по указанному пути

**Решение:**
1. Проверьте путь к файлам: `docker-compose exec app ls -la /app/src/presentation/web_app/`
2. Проверьте монтирование статических файлов в логах
3. Убедитесь, что путь в URL правильный: `/webapp/privacy_policy.html`

### Ошибка: "CORS" или "Mixed Content"

**Причина:** Проблемы с безопасностью браузера

**Решение:**
1. Убедитесь, что все ресурсы используют HTTPS
2. Проверьте заголовки CORS (если нужны)
3. Убедитесь, что CSS файл загружается по HTTPS

## Быстрое исправление:

Если ничего не помогает, выполните полную перезагрузку:

```bash
# На сервере
cd /root/Flo/Flo

# Получите последние изменения
git pull origin main

# Пересоберите и перезапустите
docker-compose down
docker-compose build app
docker-compose up -d

# Проверьте логи
docker-compose logs app --tail 50
docker-compose logs nginx --tail 50

# Проверьте доступность
curl https://your-domain.com/webapp/privacy_policy.html
```

## Контрольный список:

- [ ] `WEBHOOK_URL` установлен в `.env` с HTTPS
- [ ] Файлы `privacy_policy.html` и `style.css` существуют в `src/presentation/web_app/`
- [ ] Файлы доступны через `curl https://your-domain.com/webapp/privacy_policy.html`
- [ ] Nginx правильно проксирует запросы
- [ ] SSL сертификат валиден
- [ ] Домен доступен извне
- [ ] В логах нет ошибок монтирования статических файлов

